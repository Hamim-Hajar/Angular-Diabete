/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AddglecimieComponent } from './addglecimie.component';

describe('AddglecimieComponent', () => {
  let component: AddglecimieComponent;
  let fixture: ComponentFixture<AddglecimieComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AddglecimieComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AddglecimieComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
